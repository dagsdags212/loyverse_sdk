---
phase: 01-code-cleanup-bugfixes
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - src/loyverse_sdk/db/exporter.py
  - src/loyverse_sdk/db/connection.py
autonomous: true
requirements: [CLEANUP-01]
gap_closure: true
gaps_addressed: [CR-01, CR-02, CR-03, WR-02]

must_haves:
  truths:
    - "export_all loop catches duckdb.Error from export_resource() and wraps it in ExportError"
    - "_batch_insert catches duckdb.Error from conn.begin()/conn.commit() and wraps it in ExportError"
    - "_export_merchant catches all unexpected exceptions and wraps them in ExportError"
    - "transaction() rollback handler is narrowed to (duckdb.Error, ExportError) — no bare except Exception"
  artifacts:
    - path: "src/loyverse_sdk/db/exporter.py"
      provides: "Broadened except clauses at lines ~151, ~268, ~318"
    - path: "src/loyverse_sdk/db/connection.py"
      provides: "Narrowed rollback except clause at line ~118"
  key_links:
    - from: "exporter.py:export_all"
      to: "ExportError"
      via: "except (ExportError, duckdb.Error) wrapping"
    - from: "connection.py:transaction"
      to: "duckdb.Error"
      via: "narrowed rollback handler"
---

<objective>
Close four exception-handling gaps identified in the phase-01 human UAT review (CR-01, CR-02, CR-03, WR-02). All gaps involve either too-narrow or too-broad except clauses that leave duckdb.Error unhandled or allow bare Exception to escape untyped.

Purpose: Complete the exception tightening work begun in plans 01-02 and 01-03. After this plan the entire exception surface in exporter.py and connection.py is consistently typed, wrapped into ExportError where appropriate, and carries a resource_name for observability.
Output: Two modified source files with updated except clauses.
</objective>

<context>
@/home/dagsdags/workspace/personal-projects/loyverse_sdk/.planning/PROJECT.md
@/home/dagsdags/workspace/personal-projects/loyverse_sdk/.planning/ROADMAP.md
@/home/dagsdags/workspace/personal-projects/loyverse_sdk/.planning/STATE.md
@/home/dagsdags/workspace/personal-projects/loyverse_sdk/src/loyverse_sdk/db/exporter.py
@/home/dagsdags/workspace/personal-projects/loyverse_sdk/src/loyverse_sdk/db/connection.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Broaden except clauses in exporter.py (CR-01, CR-02, CR-03)</name>
  <files>src/loyverse_sdk/db/exporter.py</files>
  <behavior>
    CR-01: When export_resource() raises duckdb.Error, export_all wraps it into
      ExportError(str(e), resource_name=resource_name) — raw duckdb.Error never escapes.
    CR-02: When conn.begin() or conn.commit() raises duckdb.Error inside _batch_insert,
      the handler wraps it into ExportError(str(e), resource_name=resource_name) and re-raises.
    CR-03: When client.merchant.retrieve() raises any unexpected exception inside
      _export_merchant, it is caught and re-raised as
      ExportError(f"Failed to export merchant: {e}", "merchant").
      Existing ExportError instances pass through unchanged.
  </behavior>
  <action>
    Apply three surgical edits to exporter.py:

    **CR-01** — export_all loop (~line 151):
      BEFORE: `except ExportError as e:`
      AFTER:  `except (ExportError, duckdb.Error) as e:`
      Add after the except line (before existing body):
        `if not isinstance(e, ExportError):`
        `    e = ExportError(str(e), resource_name=resource_name)`
      The rest of the error-handling body is unchanged.

    **CR-03** — _export_merchant (~line 268):
      BEFORE: `except ExportError as e:`
      AFTER:  `except Exception as e:`
      Replace the body:
        `if not isinstance(e, ExportError):`
        `    raise ExportError(f"Failed to export merchant: {e}", "merchant") from e`
        `raise`

    **CR-02** — _batch_insert (~line 318):
      BEFORE: `except ExportError as e:`
      AFTER:  `except (ExportError, duckdb.Error) as e:`
      Add after the except line (before existing body):
        `if not isinstance(e, ExportError):`
        `    raise ExportError(str(e), resource_name=resource_name) from e`
      Keep the existing re-raise for the ExportError branch.

    No new imports needed — duckdb and ExportError are already imported in exporter.py.
    Do not alter any other except clauses, method signatures, or imports.
  </action>
  <verify>
    <automated>cd /home/dagsdags/workspace/personal-projects/loyverse_sdk && python -m pytest -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests pass.
    grep -n "except ExportError" src/loyverse_sdk/db/exporter.py — lines 151, 268, 318 must NOT appear as plain `except ExportError`.
    grep -n "except (ExportError, duckdb.Error)" src/loyverse_sdk/db/exporter.py — must return ≥ 2 matches.
    grep -n "except Exception" src/loyverse_sdk/db/exporter.py — must return exactly 1 match (CR-03 _export_merchant site).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Narrow rollback handler in connection.py (WR-02)</name>
  <files>src/loyverse_sdk/db/connection.py</files>
  <behavior>
    WR-02: The transaction() context manager's rollback handler must only catch
    duckdb.Error and ExportError. Unrelated exceptions (e.g. KeyError, ValueError)
    must propagate directly without triggering rollback.
  </behavior>
  <action>
    Edit connection.py around line 118 inside the transaction() context manager:
      BEFORE: `except Exception:`
      AFTER:  `except (duckdb.Error, ExportError):`

    Check if ExportError is already imported at the top of connection.py.
    If not, add:
      `from loyverse_sdk.db.exceptions import ExportError`

    duckdb is already imported. The `conn.rollback()` and bare `raise` beneath are unchanged.
    Do not alter any other method or import.
  </action>
  <verify>
    <automated>cd /home/dagsdags/workspace/personal-projects/loyverse_sdk && python -m pytest -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    All tests pass.
    grep -n "except Exception" src/loyverse_sdk/db/connection.py — must return zero matches for the rollback handler site.
    grep -n "except (duckdb.Error, ExportError)" src/loyverse_sdk/db/connection.py — must return exactly 1 match.
  </done>
</task>

<task type="auto">
  <name>Task 3: Full regression and gap-site confirmation</name>
  <files></files>
  <action>
    Run the full test suite. Then run all four gap-site grep checks:

    1. grep -n "except (ExportError, duckdb.Error)" src/loyverse_sdk/db/exporter.py  → ≥ 2 matches
    2. grep -n "except Exception" src/loyverse_sdk/db/exporter.py                    → exactly 1 match (_export_merchant)
    3. grep -n "except (duckdb.Error, ExportError)" src/loyverse_sdk/db/connection.py → exactly 1 match
    4. grep -n "except Exception" src/loyverse_sdk/db/connection.py                  → 0 matches in rollback handler

    If any check fails, correct the mismatch before committing.

    Commit with message:
      fix(01): broaden exporter catches (CR-01/02/03) and narrow connection rollback (WR-02)
  </action>
  <verify>
    <automated>cd /home/dagsdags/workspace/personal-projects/loyverse_sdk && python -m pytest -x -q 2>&1 | tail -30</automated>
  </verify>
  <done>Full suite passes. All four grep checks return expected counts. Committed.</done>
</task>

</tasks>

<success_criteria>
- CR-01: export_all no longer lets raw duckdb.Error escape the loop body
- CR-02: _batch_insert no longer lets duckdb.Error from the transaction context manager escape unwrapped
- CR-03: _export_merchant catches all unexpected exceptions and wraps them into ExportError with resource_name="merchant"
- WR-02: transaction() rollback handler is narrowed to (duckdb.Error, ExportError) — bare except Exception is gone from that site
- All existing tests continue to pass
</success_criteria>

<output>
Create `.planning/phases/01-code-cleanup-bugfixes/01-04-SUMMARY.md` when done.
</output>
